{
	admin off
	auto_https off
}

:{$PORT} {
	handle /health {
		respond "OK" 200
	}

	handle /mqtt* {
		reverse_proxy 127.0.0.1:8083
	}

	handle {
		respond "EMQX MQTT broker - WebSocket endpoint at /mqtt" 200
	}
}