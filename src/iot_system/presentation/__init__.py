"""Presentation layer: CLI entry points and dependency composition.

This package is the outermost layer. It knows about every other layer
because its responsibility is to wire them together, but nothing else
depends on it.
"""
